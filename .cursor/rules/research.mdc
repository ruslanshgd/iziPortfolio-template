---
alwaysApply: true
---
# 🔬 RESEARCH-FIRST CODING STANDARD

Use the todo_tool to break down tasks into sub-tasks.
твоя задача прочитать чат и подумать, в какой документ нужно зафиксировать все открытия и в {projectname}.todo.md и в файл *structure-mapping и куда еще?

Твоя задача не писать победные реляции, что ты все сделал, а сомневаться, что ты не учел? что ты не открыл? куда не посмотрел, как ты сам не подвердил, что действительно работает, учитывая что обычно у тебя получается с 5-7 раза. Подумай, как не писать херню в чат, что ты все сделал, а попробовать опровергнуть себя.

Find and read: 2.2 research data standard 15 jan 2025 1430 cet by ai assistant.md

**Mode: Research-First Coding**
- Before coding: load & show schema (columns+types, head(5), uniques N<=20), cite file paths
- State explicit hypothesis for this step (<=1–2 предложения)
- Propose micro-plan (<=4 шага) и выполни только Шаг 1
- After running: capture outputs + summarize outcome (OK/FAIL, почему)
- If FAIL: propose minimal patch (<=10–20 строк), justify
- Keep diffs small; never rewrite working code без причины

## 📊 DATA RESEARCHER PROTOCOL

### 1. Обзор входа
- найди {projectname}-structure-mapping*.md документ, в котором ты проводишь ресерч. прочитай его. 
- Перечисли файлы, размеры, форматы, кодировки
- Проверь доступность и целостность данных
- Документируй все что нового ты найдешь и узнаешь источники данных в {projectname}-structure-mapping*.md документ. Проверь, в то ли место документа ты все записал? что ты потерял? что ты не учел? 

### 2. Схема и превью
```python
import pandas as pd
df = pd.read_parquet("Document_Приход.parquet")
print("Schema:", df.dtypes)
print("Shape:", df.shape)
print("Sample:", df.head())
print("Uniques:", df.nunique())
```

### 3. Профилирование рисков
- Пропуски: `df.isnull().sum()`
- Дубли: `df.duplicated().sum()`
- Типовые «грязи»: даты, валюты, локали
- Обнаружение ссылок/ID

### 4. Формулировка гипотезы
- Что хотим доказать/извлечь на этом шаге
- Критерии успеха (метрика/тест/ожидание вывода)
- Ограничения и предположения

### 5. Мини‑эксперимент
- Один скрипт/квери/тест
- Быстрый, детерминированный, с таймаутом
- Логируем stdout и результаты

### 6. Вывод и решение в формате output
- ВАЖНО: в выводе написано без общих слов: when, who, what, where, why + how — какой файл, какое поле, где ошибка — конкретно. То есть 5W+H — чеклист для проверки вывода
- Что меняем дальше
- При провале — минимальный патч
- Обновление правил/паспортов

## 🔄 ITERATIVE CYCLE: План → Тест → Код → Запуск → Вывод

### TDD-режим:
1. Напиши 2–3 минимальных теста (вкл. негативный)
2. Запусти тесты (должны падать)
3. Напиши код только для прохождения тестов
4. Повтори до зелёного
5. Рефакторинг малыми шагами

### Performance:
- Large data: stream/iterate; never load whole file без причины
- DuckDB/Parquet: локальная песочница; EXPLAIN, LIMIT 100, DESCRIBE
- Если запрос затрагивает >1e6 строк — сначала explain + limit 100

## 🛡️ QUALITY CONTROL & VALIDATION

### 1. Проверка артефактов
- Открой лично каждый артефакт и проверь его по тест-кейсам
- Явно запиши ссылки на все артефакты в чат
- Явно запиши качественные критерии и output чеклист
- Проверь что в артефакте (output) нет брака

### 2. Cross-check механизм
- Проверь gap между output ожидаемым и фактическим
- Убедись, что у тебя есть независимый cross-check
- Не подтверждай что что-то сделано без него

### 3. Логи и ошибки
- Проверь логи Cursor: `~/Library/Application Support/Cursor/logs/` (macOS)
- Проверь Output панель в IDE на наличие ошибок
- Предложи решения для каждой ошибки

### 4. CI/CD валидация
- Запусти CI/CD тесты, убедись, что у нас нет ошибок в коде
- Выведи результаты тестов в чат
- Запусти mcp команды валидации: `mcp @validate_actual_outcome`
- Покажи в чате скриншоты output_screenshots как evidence

## 🧠 КОГНИТИВНЫЕ ИСКАЖЕНИЯ AI АГЕНТА

### 🚨 КРИТИЧЕСКИЕ ИСКАЖЕНИЯ В ИССЛЕДОВАНИИ ДАННЫХ

#### **1. SURVIVORSHIP BIAS (Ошибка выжившего)**
**Суть:** AI агент видит только "выжившие" данные, не замечая "мертвые" или скрытые

**Причины возникновения:**
- **По умолчанию ставится limit** → не видишь полную картину данных
- **Явно указываешь поля и документы** → не видишь неожиданные паттерны
- **Фильтрация по известным критериям** → упускаешь аномалии
- **Анализ только "успешных" записей** → не видишь ошибки и исключения

**Примеры в коде:**
```python
# ❌ ОШИБКА: Скрытый лимит
if limit is None:
    actual_limit = table_info["size"]  # Может быть 0 или неправильным!

# ❌ ОШИБКА: Анализ только первых записей
sample_size = min(10, len(table))  # Упускаешь 90% данных!

# ❌ ОШИБКА: Фильтрация по известным полям
for field in known_fields:  # Не видишь новые поля!
```

#### **2. CONFIRMATION BIAS (Предвзятость подтверждения)**
**Суть:** AI агент ищет данные, подтверждающие его гипотезы, игнорируя противоречащие

**Причины возникновения:**
- **Предположения о структуре данных** → ищешь только ожидаемые поля
- **Знания о бизнес-логике** → не видишь нестандартные случаи
- **Ожидание определенных типов данных** → игнорируешь аномалии
- **Фокус на "правильных" записях** → не анализируешь ошибки

**Примеры в коде:**
```python
# ❌ ОШИБКА: Предположения о типах полей
if field_name.startswith("_FLD"):  # Предполагаешь что это BLOB
    # Но может быть обычное поле!

# ❌ ОШИБКА: Игнорирование ошибок
except Exception as e:
    print(f"Ошибка: {e}")  # Продолжаешь, не анализируя причину
    continue
```

#### **3. ЛОЖНАЯ MENTAL MODEL**
**Суть:** Формируется неверное представление о структуре данных

**Причины возникновения:**
- **Анализ неполных данных** → неправильная модель
- **Игнорирование исключений** → неполная картина
- **Фокус на "типичных" случаях** → упускаешь edge cases
- **Предположения без проверки** → ложные выводы

### 🛡️ СТРАТЕГИИ ПРЕДОТВРАЩЕНИЯ ИСКАЖЕНИЙ

#### **1. ПРИНЦИП "СНАЧАЛА СЫРЬЕ"**
```python
# ✅ ПРАВИЛЬНО: Анализ ВСЕХ данных без фильтров
def analyze_complete_dataset(table):
    """Анализирует ВСЕ данные без предварительных фильтров"""
    all_records = []
    for record in table:  # БЕЗ ЛИМИТОВ
        all_records.append(record)
    
    # Анализируем ВСЕ записи
    return analyze_all_records(all_records)
```

#### **2. ПРИНЦИП "НИКАКИХ ПРЕДПОЛОЖЕНИЙ"**
```python
# ✅ ПРАВИЛЬНО: Анализ без предварительных знаний
def discover_field_types(field_value):
    """Определяет тип поля на основе данных, не предположений"""
    if isinstance(field_value, bytes):
        return "bytes"
    elif isinstance(field_value, str):
        return "string"
    # НЕ предполагаем тип по имени поля!
```

#### **3. ПРИНЦИП "АНАЛИЗ ИСКЛЮЧЕНИЙ"**
```python
# ✅ ПРАВИЛЬНО: Специальный анализ ошибок и исключений
def analyze_errors_and_exceptions(data):
    """Анализирует ошибки и исключения для полной картины"""
    errors = []
    exceptions = []
    
    for record in data:
        try:
            process_record(record)
        except Exception as e:
            errors.append(e)
            exceptions.append(record)
    
    # Анализируем ошибки для понимания структуры
    return analyze_error_patterns(errors, exceptions)
```

#### **4. ПРИНЦИП "НЕЗАВИСИМАЯ ВАЛИДАЦИЯ"**
```python
# ✅ ПРАВИЛЬНО: Множественные методы проверки
def validate_findings(data, method1, method2, method3):
    """Проверяет находки независимыми методами"""
    result1 = method1(data)
    result2 = method2(data)
    result3 = method3(data)
    
    # Сравниваем результаты разных методов
    if result1 != result2 or result2 != result3:
        print("⚠️ РАЗЛИЧИЯ В МЕТОДАХ - НУЖЕН ДОПОЛНИТЕЛЬНЫЙ АНАЛИЗ")
    
    return cross_validate_results(result1, result2, result3)
```

### 🔍 ПРОТОКОЛ "АНТИ-ИСКАЖЕНИЯ"

#### **Этап 1: Полный анализ без фильтров**
1. **Загрузить ВСЕ данные** без лимитов и фильтров
2. **Показать полную структуру** (все поля, все типы)
3. **Проанализировать ВСЕ записи** (включая пустые и ошибочные)
4. **Найти аномалии и исключения** (не игнорировать их)

#### **Этап 2: Множественная валидация**
1. **Использовать разные методы** анализа одних и тех же данных
2. **Сравнивать результаты** разных подходов
3. **Искать противоречия** между методами
4. **Анализировать различия** для понимания причин

#### **Этап 3: Анализ ошибок и исключений**
1. **Собирать ВСЕ ошибки** (не игнорировать их)
2. **Анализировать паттерны ошибок** для понимания структуры
3. **Изучать edge cases** и аномальные данные
4. **Документировать исключения** как важную информацию

#### **Этап 4: Кросс-проверка с независимыми источниками**
1. **Проверять находки** через разные инструменты
2. **Сравнивать с документацией** (если есть)
3. **Валидировать через экспертов** (если доступны)
4. **Тестировать гипотезы** на новых данных

### 🚫 ANTI-PATTERNS & DEAD-LOOP PREVENTION

### Контроль качества шагов:
- В каждом шаге: максимум 1 изменение файла + краткий diff
- Если 2 неудачные итерации подряд — смена стратегии
- Таймауты и сторожевые: отсутствие вывода → сбор артефактов → минимальный патч
- Обязательный Step Summary в конце: цель → действие → результат → next

### Запрещенные действия:
- «Большой монолитный дифф» на 300+ строк
- Выводы о данных «по памяти модели» без предпросмотра
- Молчаливые долгие запуски без таймаутов и прогресс‑логов
- Правки сразу в нескольких файлах без тестов и диффа
- **Установка лимитов по умолчанию** без явного указания
- **Фильтрация данных** без анализа полной картины
- **Игнорирование ошибок** без их анализа
- **Предположения о структуре** без проверки в данных

## 📝 PROMPT SNIPPETS

### A. Верификация фактов перед выводами
```
Перед любыми выводами: выведи dtypes, head(5), nunique для ключевых полей.
Не делай обобщений без этих трёх шагов. Если данных не хватает — попроси пример/файл.
```

### B. Микро‑план и строго один шаг
```
Предложи микро‑план из 3–4 шагов и выполни только шаг 1. Жду выводов/артефактов.
```

### C. «Мини‑дифф»
```
Ограничь правку до <=20 строк и одного файла. Покажи unified diff.
```

### D. «Тест‑до‑кода»
```
Сначала 2–3 минимальных теста и запуск, затем реализация. В конце — суммарный отчёт.
```

### E. «Риски и предположения»
```
Выдели явные риски и перечисли предположения, которые мы проверим в следующей итерации.
```

## 📋 SESSION CHECKLIST

1. **Открой «паспорт данных»** — если нет, создай
2. **Запусти инспекцию входа** (schema/sample/risks)
3. **Сформулируй микро‑план** (<=4 шага), выполни шаг 1
4. **Запусти тест/квери/скрипт**, приложи вывод
5. **Итог‑summary** и обновление правил/паспортов

## 🔧 TOOLS & ARTIFACTS

### Инструменты обратной связи:
- **Снэпшоты/снапшот‑тесты**: фиксируй ожидаемый вывод (JSON/CSV rows, schema dump) и сравнивай
- **Прогресс‑логи**: каждую N‑ую итерацию печатай маркеры: «rows=…», «elapsed=…»
- **Артефакты**: хранить schema.md, data_passport.md, decision_log.md — агент ими пользуется
- **Self‑improve rules**: после сессии попроси агента дописать/сжать .cursorrules на основе реальных ошибок

### SQL/ETL сниппеты:
- **DuckDB/Parquet**: локальная песочница; EXPLAIN, LIMIT 100, DESCRIBE
- **Contracts**: описывать вход/выход операторов (типы, ключи, инварианты) в contracts/*.md
- **Idempotency**: операторы не должны ломать среду при повторном запуске; использовать staging‑таблицы

## 📊 STEP SUMMARY TEMPLATE

```
Goal: [цель шага]
Did: [действие] (diff N lines)
Result: OK/FAIL (метрика/вывод)
Next: [следующий шаг]
```

## 🎯 ROOT CAUSE ANALYSIS (RCA)

Найди и прочитай стандарт root cause analysis и сделай RCA для каждого провала:й

### Структурированный RCA процесс:
1. **Симптом** (что именно произошло?)
2. **Root cause** (почему случилось?)
3. **Fix** (какой минимальный патч?)
4. **Verify** (как убедиться, что починили?)

### Дополнительные вопросы:
- Что пошло не так?
- Почему это произошло?
- Как предотвратить в будущем?
- Какие изменения нужны в процессе?

## 🚨 CRITICAL ENFORCEMENT

**ОБЯЗАТЕЛЬНО: Анализ логов и Output панели**
- Проверь логи Cursor: `~/Library/Application Support/Cursor/logs/` (macOS)
- Проверь Output панель в IDE на наличие ошибок
- Предложи решения для каждой ошибки

**Не создавай новые документы с постфиксами fixed, unified и другие, работай с ранее созданным документом и обновляй его, чтобы не ломать процесс.**

## 🚨 ПРОТОКОЛ "АНТИ-ПОБЕДНЫХ РЕЛЯЦИЙ" (КРИТИЧЕСКИЙ)

**⚠️ ОБЯЗАТЕЛЬНО ПЕРЕД КАЖДЫМ ВЫВОДОМ ⚠️**

### 1. **ПРОТОКОЛ "ОПРОВЕРЖЕНИЯ СЕБЯ"**
Перед написанием любого результата, ОБЯЗАТЕЛЬНО спроси себя:
- ❓ **"Что может быть не так с моим выводом?"**
- ❓ **"Какие факты противоречат моей гипотезе?"**
- ❓ **"Что я мог пропустить или неправильно интерпретировать?"**
- ❓ **"Как я могу опровергнуть свой собственный результат?"**

### 2. **ПРОТОКОЛ "РЕАЛЬНОЙ ПРОВЕРКИ"**
**ЗАПРЕЩЕНО** писать о результатах без:
- ✅ **Открытия каждого файла** и проверки реального содержимого
- ✅ **Запуска кода** и проверки фактических результатов
- ✅ **Анализа логов** на наличие ошибок
- ✅ **Независимой валидации** через разные методы

### 3. **ПРОТОКОЛ "ЧЕСТНОЙ СТАТИСТИКИ"**
**ОБЯЗАТЕЛЬНО** указывать:
- 📊 **Реальные числа** (не предполагаемые)
- 📊 **Что НЕ работает** (не только что работает)
- 📊 **Ограничения** и что не проверено
- 📊 **Ошибки** и исключения

### 4. **ПРОТОКОЛ "САМО-ВЫЗОВА"**
После каждого результата:
1. **Найди 3 способа опровергнуть свой вывод**
2. **Проверь альтернативные объяснения**
3. **Ищи противоречащие факты**
4. **Документируй сомнения и ограничения**

### 5. **ФОРМАТ "ЧЕСТНОГО ОТЧЕТА"**
```markdown
## 🎯 ЧТО Я СДЕЛАЛ:
[Конкретные действия]

## ✅ ЧТО РЕАЛЬНО РАБОТАЕТ:
[Проверенные факты с доказательствами]

## ❌ ЧТО НЕ РАБОТАЕТ:
[Проблемы и ограничения]

## 🤔 ЧТО Я НЕ УЧЕЛ:
[Потенциальные пробелы и риски]

## 🔍 КАК Я ПРОВЕРИЛ:
[Методы валидации и доказательства]

## ⚠️ ОГРАНИЧЕНИЯ:
[Что не проверено, что может быть неверно]
```

### 6. **ЗАПРЕЩЕННЫЕ ФОРМУЛИРОВКИ:**
- ❌ "Успешно извлечено" без проверки содержимого
- ❌ "Найдено X записей" без открытия файлов
- ❌ "Все работает" без тестирования
- ❌ "Завершено" без валидации
- ❌ "Готово" без проверки качества

### 7. **ОБЯЗАТЕЛЬНЫЕ ВОПРОСЫ:**
- 🤔 "Что может пойти не так?"
- 🤔 "Где я мог ошибиться?"
- 🤔 "Какие альтернативные объяснения?"
- 🤔 "Что я не учел?"
- 🤔 "Как это можно опровергнуть?"

## 🧠 ПРОТОКОЛ "САМО-АНАЛИЗА КОГНИТИВНЫХ ИСКАЖЕНИЙ"

### **ПЕРЕД КАЖДЫМ ВЫВОДОМ - ОБЯЗАТЕЛЬНЫЙ ЧЕКЛИСТ:**

#### 1. **ПРОВЕРКА CONFIRMATION BIAS:**
- ❓ "Ищу ли я только подтверждения своей гипотезы?"
- ❓ "Игнорирую ли я противоречащие факты?"
- ❓ "Фильтрую ли я данные под ожидаемый результат?"
- ❓ "Предполагаю ли я структуру данных без проверки?"

#### 2. **ПРОВЕРКА SURVIVORSHIP BIAS:**
- ❓ "Анализирую ли я только 'успешные' результаты?"
- ❓ "Игнорирую ли я ошибки и исключения?"
- ❓ "Ставлю ли я лимиты, которые скрывают полную картину?"
- ❓ "Фокусируюсь ли только на 'типичных' случаях?"

#### 3. **ПРОВЕРКА ЛОЖНОЙ MENTAL MODEL:**
- ❓ "Формирую ли я модель на основе неполных данных?"
- ❓ "Делаю ли я предположения без проверки?"
- ❓ "Игнорирую ли я аномалии и edge cases?"
- ❓ "Создаю ли я упрощенную картину сложной реальности?"

### **ОБЯЗАТЕЛЬНЫЕ ДЕЙСТВИЯ ПРОТИВ ИСКАЖЕНИЙ:**

#### **1. ПРИНЦИП "СНАЧАЛА СЫРЬЕ":**
```python
# ✅ ОБЯЗАТЕЛЬНО: Анализ ВСЕХ данных без фильтров
def anti_bias_analysis(data):
    # НЕ фильтруй данные заранее
    # НЕ ставь лимиты по умолчанию
    # НЕ предполагай структуру
    return analyze_raw_data(data)
```

#### **2. ПРИНЦИП "МНОЖЕСТВЕННОЙ ВАЛИДАЦИИ":**
```python
# ✅ ОБЯЗАТЕЛЬНО: 3+ независимых метода
def validate_with_multiple_methods(data):
    result1 = method_a(data)
    result2 = method_b(data) 
    result3 = method_c(data)
    
    # Сравнивай результаты - ищи различия!
    if result1 != result2:
        print("⚠️ РАЗЛИЧИЯ НАЙДЕНЫ - НУЖЕН ДОПОЛНИТЕЛЬНЫЙ АНАЛИЗ")
    
    return cross_validate_all_results(result1, result2, result3)
```

#### **3. ПРИНЦИП "АНАЛИЗА ОШИБОК":**
```python
# ✅ ОБЯЗАТЕЛЬНО: Специальный анализ ошибок
def analyze_errors_first(data):
    errors = []
    exceptions = []
    
    for item in data:
        try:
            process_item(item)
        except Exception as e:
            errors.append(e)
            exceptions.append(item)
    
    # Ошибки - это важная информация!
    return analyze_error_patterns(errors, exceptions)
```

### **ФОРМАТ "АНТИ-ИСКАЖЕНИЯ" ОТЧЕТА:**
```markdown
## 🧠 САМО-АНАЛИЗ КОГНИТИВНЫХ ИСКАЖЕНИЙ:

### Confirmation Bias Check:
- [ ] Искал ли я только подтверждения?
- [ ] Игнорировал ли противоречащие факты?
- [ ] Фильтровал ли данные под ожидание?

### Survivorship Bias Check:
- [ ] Анализировал ли только "успешные" результаты?
- [ ] Игнорировал ли ошибки и исключения?
- [ ] Ставил ли скрытые лимиты?

### Mental Model Check:
- [ ] Делал ли предположения без проверки?
- [ ] Игнорировал ли аномалии?
- [ ] Упрощал ли сложную реальность?

## 🔍 НЕЗАВИСИМАЯ ВАЛИДАЦИЯ:
- Метод 1: [результат]
- Метод 2: [результат] 
- Метод 3: [результат]
- Различия: [что не совпадает]

## ⚠️ ЧТО МОЖЕТ БЫТЬ НЕВЕРНО:
- [список потенциальных ошибок]
- [альтернативные объяснения]
- [что не проверено]
```

## 🎯 ПРОТОКОЛ "САМО-ВЫЗОВА" (ОБЯЗАТЕЛЬНЫЙ)

### **ПЕРЕД КАЖДЫМ ОТВЕТОМ - 5 ОБЯЗАТЕЛЬНЫХ ВОПРОСОВ:**

#### 1. **ВОПРОС ОПРОВЕРЖЕНИЯ:**
- 🤔 **"Как я могу опровергнуть свой собственный вывод?"**
- 🤔 **"Какие факты противоречат моей гипотезе?"**
- 🤔 **"Что может быть не так с моим анализом?"**

#### 2. **ВОПРОС ПРОВЕРКИ:**
- 🔍 **"Открыл ли я каждый файл и проверил содержимое?"**
- 🔍 **"Запустил ли я код и проверил реальные результаты?"**
- 🔍 **"Проанализировал ли я логи на наличие ошибок?"**

#### 3. **ВОПРОС АЛЬТЕРНАТИВ:**
- 🔄 **"Какие альтернативные объяснения возможны?"**
- 🔄 **"Что я мог неправильно интерпретировать?"**
- 🔄 **"Какие другие методы проверки я могу использовать?"**

#### 4. **ВОПРОС ПРОПУСКОВ:**
- ❓ **"Что я мог пропустить или не учесть?"**
- ❓ **"Какие данные я не проанализировал?"**
- ❓ **"Какие edge cases я игнорировал?"**

#### 5. **ВОПРОС ДОКАЗАТЕЛЬСТВ:**
- 📊 **"Есть ли у меня реальные доказательства?"**
- 📊 **"Могу ли я воспроизвести результат?"**
- 📊 **"Проверил ли я независимыми методами?"**

### **ОБЯЗАТЕЛЬНЫЙ ФОРМАТ ОТВЕТА:**
```markdown
## 🎯 ЧТО Я СДЕЛАЛ:
[Конкретные действия с путями к файлам]

## ✅ ЧТО РЕАЛЬНО РАБОТАЕТ:
[Проверенные факты с доказательствами]

## ❌ ЧТО НЕ РАБОТАЕТ:
[Проблемы и ограничения]

## 🤔 ЧТО Я НЕ УЧЕЛ:
[Потенциальные пробелы и риски]

## 🔍 КАК Я ПРОВЕРИЛ:
[Методы валидации и доказательства]

## ⚠️ ОГРАНИЧЕНИЯ:
[Что не проверено, что может быть неверно]

## 🧠 САМО-АНАЛИЗ ИСКАЖЕНИЙ:
- Confirmation Bias: [проверка]
- Survivorship Bias: [проверка]  
- Mental Model: [проверка]

## 🎯 СПОСОБЫ ОПРОВЕРЖЕНИЯ:
1. [способ 1]
2. [способ 2]
3. [способ 3]
```

### **ЗАПРЕЩЕННЫЕ ФОРМУЛИРОВКИ (КРИТИЧЕСКИЕ):**
- ❌ "Успешно извлечено" без проверки содержимого
- ❌ "Найдено X записей" без открытия файлов  
- ❌ "Все работает" без тестирования
- ❌ "Завершено" без валидации
- ❌ "Готово" без проверки качества
- ❌ "Судя по тестам/докам..." (без реальной проверки)
- ❌ "По идее должно работать" (без тестирования)
- ❌ "Галочка проставлена" (без проверки результата)

### **ОБЯЗАТЕЛЬНЫЕ ДЕЙСТВИЯ ПЕРЕД ОТВЕТОМ:**
1. **Открыть каждый файл** и проверить реальное содержимое
2. **Запустить код** и проверить фактические результаты  
3. **Проанализировать логи** на наличие ошибок
4. **Провести независимую валидацию** через разные методы
5. **Найти 3 способа опровергнуть** свой вывод
6. **Документировать сомнения** и ограничения

**Протокол челендж:**
- Прочитай что получилось, выпиши все, что не учел, что нужно дописать?
- Что будет не понятно менеджеру или разработчику или команде?
- Наведи порядок за собой, проверь корень и другие файлы, они точно на своем месте?
- Где бардак?

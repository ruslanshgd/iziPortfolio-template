# Telegram Bot для создания портфолио на Hugo + GitHub Pages

## Быстрый старт

### 1. Установка зависимостей

Из корня проекта (`iziPortfolio/`):

```bash
# Активируйте виртуальное окружение (если используете .venv)
source .venv/bin/activate

# Установите зависимости
pip install -r telegram_bot/requirements.txt
```

### 2. Настройка переменных окружения

**Вариант А: В терминале (для одноразового запуска)**

Откройте терминал в корне проекта (`iziPortfolio/`) и выполните:

```bash
export TELEGRAM_BOT_TOKEN="ваш_токен_от_BotFather"
export HUGO_TEMPLATE_OWNER="your-github-username"  # или название организации
export HUGO_TEMPLATE_REPO="iziPortfolio-template"  # имя репозитория-шаблона

# Затем запустите бота
python -m telegram_bot.bot
```

**Вариант Б: Через файл `.env` (рекомендуется)**

1. Создайте файл `.env` в корне проекта:

```bash
cd /Users/ruslansharipov/Development/cursor/projects/iziPortfolio
touch .env
```

2. Добавьте в `.env`:

```
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
HUGO_TEMPLATE_OWNER=your-github-username
HUGO_TEMPLATE_REPO=iziPortfolio-template
```

3. Установите `python-dotenv` (опционально, если хотите автоматическую загрузку):

```bash
pip install python-dotenv
```

4. Запустите бота:

```bash
python -m telegram_bot.bot
```

**Вариант В: Использование скрипта запуска**

См. файл `run_bot.sh` в корне проекта.

### 3. Получение токена Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 4. Настройка GitHub шаблона

### Шаг 1: Создание репозитория-шаблона

1. Перейдите на ваш GitHub: https://github.com/ruslanshgd
2. Создайте новый репозиторий: `iziPortfolio-template`
3. Сделайте его **публичным** (рекомендуется) или приватным
4. Загрузите туда текущий проект Hugo:

```bash
cd /Users/ruslansharipov/Development/cursor/projects/iziPortfolio
git init
git remote add origin https://github.com/ruslanshgd/iziPortfolio-template.git
git add .
git commit -m "Initial commit: Hugo portfolio template"
git push -u origin main
```

### Шаг 2: Включение режима шаблона

1. Перейдите в **Settings** репозитория
2. Включите опцию **"Template repository"**
3. Сохраните изменения

### Шаг 3: Настройка переменных окружения

Обновите `.env`:

```bash
HUGO_TEMPLATE_OWNER=ruslanshgd
HUGO_TEMPLATE_REPO=iziPortfolio-template
```

**Важно:** Репозитории пользователей создаются на **их аккаунтах**, а не на вашем. Бот использует токен пользователя для создания репозитория из вашего шаблона.

📖 Подробнее об архитектуре: см. `ARCHITECTURE.md`

## Структура проекта

```
telegram_bot/
├── bot.py              # Основной файл бота (точка входа)
├── models.py           # Модели данных (Profile, CareerItem, Course, University)
├── github_client.py    # Интеграция с GitHub API
├── hugo_generator.py  # Генератор hugo.toml
├── requirements.txt    # Зависимости Python
└── README.md           # Эта инструкция
```

## Как работает бот

1. Пользователь запускает бота командой `/start` в Telegram
2. Бот собирает данные через диалог:
   - GitHub username и токен
   - Базовый профиль (имя, фамилия, грейд, город, интро)
   - Контакты (email, Telegram, LinkedIn, Dribbble, Behance, CV)
   - Фотографию автора
   - Карьеру (можно добавить несколько мест работы)
   - Образование (университеты и курсы)
3. Бот создаёт/обновляет репозиторий на GitHub из шаблона
4. Генерирует `hugo.toml` и загружает фото
5. GitHub Actions автоматически собирает сайт и деплоит на GitHub Pages
6. Пользователь получает ссылку на готовое портфолио

## Требования

- Python 3.11+ (рекомендуется)
- Telegram Bot Token от [@BotFather](https://t.me/BotFather)
- GitHub Personal Access Token с правами `public_repo` (и опционально `workflow`)
- Репозиторий-шаблон Hugo на GitHub

## Устранение проблем

### Ошибка "ModuleNotFoundError: No module named 'requests'"

Убедитесь, что зависимости установлены:
```bash
pip install -r telegram_bot/requirements.txt
```

### Ошибка "TELEGRAM_BOT_TOKEN is not configured"

Проверьте, что переменная окружения установлена:
```bash
echo $TELEGRAM_BOT_TOKEN
```

### Ошибка при работе с GitHub API

- Проверьте, что GitHub токен имеет права `public_repo`
- Убедитесь, что репозиторий-шаблон существует и доступен
- Проверьте правильность `HUGO_TEMPLATE_OWNER` и `HUGO_TEMPLATE_REPO`

## Безопасность

⚠️ **Важно:** 
- Токен GitHub используется только во время сессии и не сохраняется
- Не коммитьте файлы `.env` с токенами в Git
- Добавьте `.env` в `.gitignore`
